# Project instructions

## Shorthand

- `cp` means **commit and push** the current changes. Only do this when the
  user explicitly asks for it (typing `cp` or asking to commit and push) —
  never commit or push proactively on your own.
